doctype html
html(lang="ko")
  head
    title #{pageTitle} | Wetube
    link(rel="stylesheet" href="https://unpkg.com/mvp.css")
  body 
    header
      h1 #{pageTitle}
      nav
        ul
          li 
            a(href="/") Home
          if isLoggedIn
            li
              a(href="/profile") Profile
          else
            li 
              a(href="/join") Join
            li 
              a(href="/login") Login
          li 
            a(href="/movies/upload") Upload Video
          li 
            a(href="/movies/search") Search
    
    main
      block content 
  include ../partials/footer.pug